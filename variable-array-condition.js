var botolColor = "yellow";
var waterQuantity = 1;
var isFll = false;

// array 
var items = ['bottle', 'mag', 'paper', 'pen'];
items.indexOf('mag');
items.push('pencil');
items.push('daster');
items.pop('mag');


// Conditional
 if(items.length >= 4);{
 console.log('You have too many stafs');
 }